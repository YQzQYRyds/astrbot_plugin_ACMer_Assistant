# 验证记录

验证日期：2026-09-22。开发环境：Windows，Python 3.13.12。

- `python -m pytest -q`：67 passed，1.17 秒。覆盖五个平台解析、错误响应、配置校验、时区和日期边界、重启防重、多节点合并、改期、过期缓存、目标隔离、发送失败与超时、取消恢复、日报分页与失败页补发、并发 claim、生命周期和管理员指令契约。
- `ruff check .`：通过。
- `ruff format --check .`：21 个 Python 文件通过。
- `compileall`：业务模块及入口通过。
- 安装 ZIP：CRC 检查通过，包含运行入口、配置、元信息、依赖、全部业务模块、README 与 LICENSE；不包含虚拟环境、研究文件或数据库。

实时源检查在 2026-09-22 13:08:48 UTC（北京时间 21:08:48）完成，未使用环境代理，也未发送任何群消息：

| 数据源 | 解析数量 | 其中未开赛 |
|---|---:|---:|
| Codeforces | 8 | 8 |
| AtCoder | 8 | 8 |
| 牛客 | 16 | 6 |
| LeetCode 中国站 | 2 | 2 |
| 洛谷（最多三页） | 60 | 15 |

这些是本次检查的快照，未来数量会变化。可运行 `python scripts/smoke_sources.py` 再次核验。

尚未执行：完整 AstrBot 安装启动、真实聊天适配器主动发送、长时间在线运行。生命周期与指令权限测试采用 API 契约替身，不能代替部署联调。正式发布插件市场前还需设置实际作者与仓库链接。

## v1.1.0 本次更新验证

新增验证：无任何群消息/绑定记录时 OneBot 群列表自动启用、删除白名单即停用、旧会话配置不能绕过白名单、多机器人去重、群列表失败重试、退出群后路由清理、私聊/未启用群所有指令静默、`/比赛` 别名、比赛数量上限、CF 两组正确合并与错误条件隔离、时长与空行排版、分页保留日期和完整条目。

`DISPLAY_PREVIEW.txt` 为固定示例数据生成的排版预览，不是实时赛历。此次修改未涉及赛事数据源，沿用上文已有实时检查记录；新群自动发现使用当前官方接口源码核对和契约替身测试，尚未连接真实机器人联调。

## v1.2.0 验证

新增验证：群聊与私聊身份隔离、完整会话 ID 的平台隔离、旧群号与平台白名单转换、新配置优先级、好友列表自动发现、私聊目标去重、未观察会话的完整 ID 推送、删除会话停用、数据库改名复制及已发记录保留、已有新数据库不覆盖、关闭平台不抓取且不读取其缓存。安装包更名为 `astrbot_plugin_acmer_assistant.zip`。真实机器人联调尚未执行。
